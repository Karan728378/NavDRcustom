# NavDR working constraints

The user approved building the Kotlin-native product. Preserve the distinction between the browser workbench, the native classical baseline, and future trained-model/field evidence.

**Training hardware has only 6 GB VRAM.** Start new training configurations with a small complete forward/backward/optimizer smoke test and measure peak memory before scaling. Keep data on CPU, use small microbatches and gradient accumulation where appropriate, and run one GPU job at a time by default. Do not assume a larger GPU or launch paid cloud jobs without a separate user decision. The full policy and current environment limitations are in `docs/DEVELOPMENT_CONSTRAINTS.md`.

Native checks: from `native/android`, run `./gradlew :navigation:test :app:assembleDebug :app:lintDebug`. From the repository root, run `python3 tests/native-importer.test.py` for the recording bridge. Refer to `docs/NATIVE_BUILD_PROGRESS.md` for what has actually been verified. Never label synthetic data, a software GNSS mask, or phone GNSS as independent field ground truth.

The standalone ML CLI now uses `ml/train.py --split-manifest ...` / `ml/evaluate.py --split-manifest ...`, documented fully in `ml/README.md`. The approved raw manifest schema is `navdr.approved-raw-split.v1`; no real manifest exists. `--self-test --output-dir NEW_DIR` generates explicitly synthetic pairs/fixture metadata and exercises parsing, CPU training/resume, and the actual Kotlin comparator. Latest evidence is `docs/evidence/ml-standalone-final/acceptance.json`. This does not authorize real training, approval fabrication, GPU driver changes, or a production architecture decision.

## User execution boundaries (16 September 2026)

Show actual acceptance output before calling a result verified; say untested when it has not run. Cite code file/line evidence. Do not infer authenticated PS text or deadlines. The user owns phone tests, reference collection, real-data training/interpretation, final model/ESKF/runtime decisions, pairing and split approval. The supplied `IO-VNBD-master` initially contained CSV LFS pointers; subsequent retrieval materialized all 564 CSV paths (329 unique payloads), verified against the original pointer hashes. R07 now has a real-data prefix inventory, but zero approved pairings and no real split or training. See `docs/IO_VNBD_REVIEW.md` and `docs/evidence/io-vnbd-inventory/payload-verification.json`. Human review of units, axes, clocks, duplicate copies and trustworthy pairings remains required. Synthetic toy pipeline tests and random-weight conversion of the existing JS TCN are authorized; these do not establish field accuracy. Stop at draft real-data split review before training. GPU-budget failure/driver unavailability must never be reported as a passing memory probe. See `ml/README.md` and `docs/TOOLS_ACCEPTANCE_GUIDE.md`.
